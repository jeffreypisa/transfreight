{% extends "base.twig" %}

{% block content %}
  <div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
      <section class="mod-pageheader section js-fullscreen d-flex align-items-center inverted">
        <div class="bg" style="background-image: url({{ options.achtergrond404 }})"></div>
        <div class="container">
          <h1 class="article-h1">{{ options.titel404 }}</h1>
        </div>
      </section>
      <section>
        <div class="container">
          <div class="text-center">
            <h2 class="text-center"> {{ options.tekst404 }} </h2>
            <a href="/" class="btn btn-secondary"> {{ options.knoptekst404 }}</a>
          </div>
        </div>
      </section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}